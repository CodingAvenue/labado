{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <div class="container mg-top">
        <div class="labado-item-container">
            <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
            <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
            <a class="btn" style="background-color:#3f51b5" href="{{ path('fos_user_profile_edit') }}">Edit Profile</a>
            <a class="btn" style="background-color:#3f51b5" href="{{ path('userInfoEdit') }}">Edit Information</a>
        </div>
    </div>
</div>
